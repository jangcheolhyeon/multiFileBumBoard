<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="egovframework.bumilboard.sample.vo.AnswerVo">
	
	<select id="selectBoardAnswer" parameterType="int" resultType="AnswerVo">
		select * from answer_table
		where board_id = #{board_id}
	</select>
	
	<insert id="writeAnswer" parameterType="AnswerVo">
		insert into answer_table (board_id, writer_id, reg_date, content)
		values (#{board_id}, #{writer_id}, now(), #{content})
	</insert>
	
	<select id="maxAnswerId" resultType="int">
		select answer_id from answer_table
		order by answer_id desc
		limit 1
	</select>
	
	<!-- 답글 삭제 -->
	<delete id="deleteAnswer" parameterType="int">
		delete from answer_table
		where board_id = #{board_id}
	</delete>
	
	
	<!-- 답글 변경 -->
	<update id="updateAnswer" parameterType="AnswerVo">
		update answer_table
		set content = #{content}, reg_date = now()
		where board_id = #{board_id}
	</update>
</mapper>
